# Everything `bun scripts/generate.ts` emits: it's regenerated on every build,
# and committing it would only produce hundred-line diffs on every spec change.
/public/openapi.json
/public/llms.txt
/public/llms-full.txt
/src/content/docs/reference/

node_modules/
dist/
.astro/
