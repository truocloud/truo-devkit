{
  "name": "@truocloud/codegen",
  "version": "0.0.0",
  "private": true,
  "description": "Devkit generators: the types, the SDK and the CLI command tree come from the OpenAPI spec",
  "type": "module",
  "scripts": {
    "gen": "bun src/run.ts",
    "check": "bun src/run.ts --check"
  }
}
