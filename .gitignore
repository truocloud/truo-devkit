# CLAUDE.md queda SOLO en local: describe como este repo se relaciona con la
# infraestructura interna, y este repositorio es publico.
/CLAUDE.md

node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example

# Binarios compilados del CLI (se publican como release assets, no se commitean)
/packages/cli/bin/
/release/
# Formula de brew y manifiesto de scoop: se generan en el release
/dist-packagers/

.DS_Store
Thumbs.db
/img/packages/url/fixtures/
# Declaraciones emitidas por el build; viven en dist/, nunca junto a las fuentes.
/img/packages/*/src/*.d.ts
/img/unpic-src/dist/
