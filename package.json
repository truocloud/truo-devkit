{
  "name": "truo-devkit",
  "version": "0.0.0",
  "private": true,
  "description": "SDK, CLI and (soon) MCP server for the TruoCloud public API",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "gen": "bun packages/codegen/src/run.ts",
    "gen:check": "bun packages/codegen/src/run.ts --check",
    "sync:spec": "bun packages/openapi/scripts/sync.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "bun packages/cli/scripts/build.ts",
    "build:npm": "bun scripts/build-npm.ts",
    "version:set": "bun scripts/set-version.ts",
    "version:check": "bun scripts/set-version.ts --check",
    "//test": "Scoped to packages/: `bun test` from the root also walks img/, which is a separate workspace with its own dependencies and its own CI. It passes locally (img/node_modules exists) and fails in CI, which is the worst version of that mistake.",
    "test": "bun test packages"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@types/node": "^22.10.0",
    "typescript": "^5.9.3"
  }
}
